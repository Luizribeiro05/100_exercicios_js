var largura = 22.3;

const calcularLargura = (largura) =>{
    let larguraAoCubo = largura ** 3;

    return console.log(`A largura ao cubo é : ${larguraAoCubo.toFixed(2)}`);
}

calcularLargura(largura);