var altura = 61.5;

const calcularAltura = (altura) =>{
    let alturaAoQuadrado = altura ** 2;

    return console.log(`A altura ao quadrado é : ${alturaAoQuadrado.toFixed(2)}`);
}

calcularAltura(altura);